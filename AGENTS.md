# 프로젝트 작업 규칙

## 빌드와 디스크 정리 (필수)

- 이 게임은 Phaser + SvelteKit/Vite 기반 브라우저 게임이다. 모바일 전용이나 네이티브 엔진 프로젝트로 가정하지 않는다. 플랫폼/도구가 추가되면 해당 설정과 출시 상태부터 확인한다.
- 빌드는 `npm run build`, 미리보기는 `npm run preview`를 사용한다. 새 빌드·패키징 도구와 CI도 `tools/build_lifecycle.py`의 소유 표식, 운영체제 잠금, 전용 작업 공간, 검증 후 게시, 종료 시 정리 정책을 사용해야 한다. `vite build` 직접 호출이나 `build/` 직접 덮어쓰기로 우회하지 않는다.
- `build/`는 정상 패키지를 가리키는 링크다. 개발용 성공 패키지는 최근 2회, 원문 빌드 로그는 최근 10회·각 최대 1MiB를 보존한다. 출시본·패치 기준·롤백본·심볼에는 이 개수 제한을 적용하지 않는다.
- 새 패키지 검증 전에 기존 정상 결과물을 삭제하지 않는다. `.build-work/lock`은 영구 잠금 파일이므로 실행 중이든 종료 후든 삭제하지 않는다. SIGKILL/전원 차단 뒤에는 `npm run build:clean` 또는 다음 빌드가 잠금과 소유 표식을 확인해 회수한다.
- 원본 `src/`, `static/`, 설정, 의존성, 플러그인, 서명/인증 정보, 사용자 저장 데이터와 Git 저장소는 정리 대상이 아니다. 무시 규칙·이름·확장자·수정 시각만으로 삭제하지 않는다. 프로젝트 밖 공유 캐시는 임의로 정리하지 않는다.
- 날짜·번호만 붙인 패키지와 압축 해제 사본을 사유 없이 복제하지 않는다. 디버그 사본은 복사 전에 목적, 담당자, 정확한 위치, 삭제 시점/조건을 `docs/build-debug-holds.md`에 기록한다. 자동 정리 경로 안에서 임의로 파일을 붙잡아 두지 않는다.
- 삭제 범위·이유·측정치를 기록하고 중요한 보존 파일은 전후 해시로 검증한다. 자동 정리 실패/불명확한 소유권은 남은 경로를 포함한 경고로 보고한다.
- 빌드 도구 변경 시 `npm run test:build-tools`, `npm run check`, `npm run lint`를 실행한다. 변경에 맞는 대표 빌드와 미리보기 검증을 하되 대용량 빌드를 보관 개수 테스트용으로 반복하지 않는다.

세부 정책·예외·실행 방법: [docs/build-cleanup.md](docs/build-cleanup.md).
